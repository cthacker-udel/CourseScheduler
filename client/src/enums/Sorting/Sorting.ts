/* eslint-disable no-unused-vars -- does not apply */
/* eslint-disable no-shadow -- does not apply */
/* eslint-disable no-magic-numbers -- no magic numbers, as the type names define what the number represents */

enum SORTING {
    DECREMENTING = -1,
    INCREMENTING = 1,
    NEUTRAL = 0,
}

enum TRAJECTORY {
    INCREMENTING = 1,
    DECREMENTING = -1,
    NEUTRAL = 0,
}

export { SORTING, TRAJECTORY };
