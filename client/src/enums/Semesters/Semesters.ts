/* eslint-disable no-unused-vars -- not needed for this */
/* eslint-disable no-magic-numbers -- not needed for this */
/* eslint-disable no-shadow -- not needed for this */
export enum SEMESTERS {
    SPRING = 0,
    SUMMER = 1,
    FALL = 2,
    WINTER = 3,
}
